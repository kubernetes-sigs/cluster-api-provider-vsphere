---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: tagpolicies.vsphere.policy.vmware.com
spec:
  group: vsphere.policy.vmware.com
  names:
    kind: TagPolicy
    listKind: TagPolicyList
    plural: tagpolicies
    singular: tagpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TagPolicy is the schema for the TagPolicy API and
          represents the desired state and observed status of a TagPolicy
          resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TagPolicySpec defines the desired state of TagPolicy.
            properties:
              tags:
                description: |-
                  Tags specifies the UUIDs of the vSphere tags that are applied to a
                  workload as a result of this policy.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
            type: object
          status:
            description: TagPolicyStatus defines the observed state of TagPolicy.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
